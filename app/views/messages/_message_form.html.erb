<%= form_for message, remote: true do |f| %>
  <%= f.text_field :content, data: { textfield: "message" } %>
  <%= f.hidden_field :room_id, value: room.id %>
  <%= f.submit :send, data: { send: "message" } %>
<% end %>
